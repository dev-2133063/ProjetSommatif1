<Window x:Class="WpfApp1.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1.View"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="3">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="2">
            <local:Login Visibility="Visible"></local:Login>
            <local:ModLivre Visibility="Collapsed"></local:ModLivre>
        </Grid>

        <!-- bas de l'écran -->
        <Grid Grid.Row="1" Margin="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2.5*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="175"/>
                <ColumnDefinition Width="150"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="textBlockNotifs" FontSize="14">Zone de notifications</TextBlock>

            <ComboBox x:Name="comboBoxLangue" Grid.Column="3"
                      FontSize="15" VerticalAlignment="Center">
                <ComboBoxItem Content="Français" IsSelected="True"/>
                <ComboBoxItem Content="English"/>
            </ComboBox>

            <TextBlock x:Name="textBlockTheme" 
                       Grid.Column="2" FontSize="14">Theme</TextBlock>
        </Grid>
    </Grid>
</Window>
